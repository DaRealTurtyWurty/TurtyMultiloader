{
    "schemaVersion": 1,
    "id": "gasapi",
    "version": "${version}",
    "name": "TurtyMultiloader Gas",
    "description": "Optional gas resource support for TurtyMultiloader.",
    "authors": [
        "${mod_author}"
    ],
    "license": "${license}",
    "environment": "*",
    "entrypoints": {
        "main": [
            "dev.turtywurty.gasapi.fabric.GasApiFabric"
        ]
    },
    "depends": {
        "fabricloader": ">=${fabric_loader_version}",
        "minecraft": "~${minecraft_version}",
        "turtymultiloader": ">=${version}",
        "java": ">=${java_version}"
    }
}
